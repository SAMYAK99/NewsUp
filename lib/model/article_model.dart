class ArticleModel {
  String title, description, url, content;
  String urlToImage;

  // creating a constructor for aricle news
  ArticleModel({
    this.title,
    this.description,
    this.url,
    this.urlToImage,
    this.content,
  });
}
