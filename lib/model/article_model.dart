class ArticleModel {
  String title, description, url, content;
  String urlToImage, source;

  // creating a constructor for aricle news
  ArticleModel(
      {this.title,
      this.description,
      this.url,
      this.urlToImage,
      this.content,
      this.source});
}
